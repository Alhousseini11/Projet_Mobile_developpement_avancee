# Docker Compose development variables.
# DATABASE_URL targets the postgres service inside the Docker network.
POSTGRES_DB=mon_app
POSTGRES_USER=mon_user
POSTGRES_PASSWORD=change-me-dev-password
DATABASE_URL=postgresql://mon_user:change-me-dev-password@postgres:5432/mon_app?schema=public
