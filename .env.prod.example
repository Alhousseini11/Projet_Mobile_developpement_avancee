# Docker Compose production variables.
# Replace every placeholder before deploying to a real server.
POSTGRES_DB=mon_app
POSTGRES_USER=mon_user
POSTGRES_PASSWORD=replace-with-strong-password
DATABASE_URL=postgresql://mon_user:replace-with-strong-password@postgres:5432/mon_app?schema=public
ADMIN_WEB_API_URL=https://api.garageplus.org
