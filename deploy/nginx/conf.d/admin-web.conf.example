upstream garage_admin_web {
  server admin-web:80;
}

server {
  listen 80;
  server_name admin.garageplus.org;

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl http2;
  server_name admin.garageplus.org;

  ssl_certificate /etc/nginx/certs/admin.garageplus.org.fullchain.pem;
  ssl_certificate_key /etc/nginx/certs/admin.garageplus.org.privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers off;

  location / {
    proxy_pass http://garage_admin_web;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
