# Example for local backend development.
# Android emulator usually reaches the host machine through 10.0.2.2.
# Update the host if you use the iOS simulator, a physical device, or a remote backend.
NS_API_BASE_URL=http://10.0.2.2:3000/api
